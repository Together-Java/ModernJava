# Character

The type to use for a `char` that might be null is `Character`.

```java, no_run
void main() {
    Character c = null;
    System.out.println(c);
    c = '%';
    System.out.println(c);
}
```